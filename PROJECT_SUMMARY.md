# זמני נצח (Zmanei_Nezach) - סיכום פרויקט

## תיאור כללי
כלי ווב חינמי שבו **כל משפחה** מייצרת לוח זמני כניסת ויציאת שבת (וחגים וצומות) מעוצב, לזכר יקירהּ. המשתמש עובר אשף חד-פעמי של 6 צעדים — תמונה, פרטי הנצחה, משפט לזיכרון, ערים, עיצוב, סגנון כתב — וההגדרות הופכות לקבועות במכשיר. מכאן ואילך: בוחרים תאריך, "צור לוח", ומקבלים PNG בגודל 1254×1254 לשיתוף בוואטסאפ או להדפסה.

**האתר החי:** https://idoyan-spec.github.io/Zmanei_Nezach/

**עיקרון מנחה — אפס עלות תפעול:** התמונה וההגדרות נשמרות רק בדפדפן של המשתמש, הציור נעשה במעבד שלו, והאתר סטטי לחלוטין. אין שרת, אין מסד נתונים, אין הרשמה ואין מפתחות API. ראו "כלכלת הכלי".

## קבצים עיקריים
| קובץ | תפקיד |
|-------|--------|
| `index.html` | כל הממשק: דף הכניסה המסביר, מסך הלוח, אשף 6 הצעדים, מודאל מקורות הזמנים, מערכת עיצוב מבוססת CSS custom properties |
| `app.js` | הלב: **מנוע רקעים וקטורי** (10 קומפוזיציות × 14 פלטות), אשף, מנוע Hebcal (שבת/חג/צום/יו"כ), ציור הלוח על Canvas, חיפוש יישובים, ייצוא/ייבוא הגדרות |
| `cities-il.json` | **1,174 יישובי ישראל** עם שמות בעברית וקואורדינטות WGS84 (~21KB דחוס). מקור: שכבת `CITY` של data.gov.il/מערך הדיגיטל הלאומי |
| `fonts/` | David Libre, Gveret Levin, Frank Ruhl Libre — כולם OFL |
| `demo.jpg` | צללית ניטרלית לבדיקות `?demo=1` — לא תמונה של אדם אמיתי |
| `icon.ico` / `icon-192.png` / `icon-256.png` | נר נצח בתוך קשת |

## טכנולוגיות בשימוש
- HTML/JS + Canvas בלבד. ללא ספריות, ללא שלב build, ללא תלויות התקנה.
- **Hebcal API** לזמנים — `https://www.hebcal.com/hebcal?...` (ללא מפתח, CORS פתוח, CC BY 4.0)
- **חיפוש יישובים בשלוש שכבות**: `cities-il.json` מקומי → Open-Meteo Geocoding (חו"ל) → Nominatim/OSM (מוצא אחרון, לשמות עממיים)
- **Google Fonts** ל-Suez One / Bellefair / Secular One / Heebo / Amatic SC
- אחסון: `localStorage` בלבד

## איך להשתמש
### הרצה מקומית
אין תלויות: `python -m http.server 8632` בתיקייה, ואז `http://127.0.0.1:8632/`

### פרמטרים לבדיקה
- `?reset=1` — איפוס ופתיחת האשף
- `?demo=1&auto=1&date=YYYY-MM-DD&tpl=<עיצוב>&font=<זוג>&palette=<גוון>` — יצירה אוטומטית
- עיצובים: `mist halo bloom waves dunes strata aurora veins orbit arch`
- גוונים: `navy olive stone amber sage tchelet wine rose plum forest slate sand midnight charcoal`
- גופנים: `classic festive elegant modern script`

### בדיקה חיה בדפדפן (CDP, ללא תלויות)
Chrome עם `--remote-debugging-port` + סקריפט Node שמשתמש ב-WebSocket המובנה. ראו `scratchpad/cdp-test.js` בסשן.

### פריסה
`git push` — GitHub Pages מתעדכן תוך כדקה. **תמיד לאמת אחרי פריסה**: `curl -s <url>/app.js | grep BUILD`

## כלכלת הכלי (אומת מול תנאי השירותים)
| שירות | מגבלה | סיכון |
|--------|--------|-------|
| GitHub Pages | 100GB/חודש (רכה), 1GB אתר (קשיחה) | **אין חיוב אפשרי** — חריגה מייצרת מייל, לא חשבונית |
| Hebcal | 90 בקשות ל-10 שניות, ללא מפתח | זניח (0.07 בקשות/שנייה גם ב-5,000 משפחות) |
| Open-Meteo | 10,000/יום, ללא מפתח | זניח. **חינם רק לשימוש לא-מסחרי** — פרסומות או מנוי יחייבו תשלום |
| Nominatim | בקשה אחת לשנייה **לכל אפליקציה**; חיפוש-תוך-כדי-הקלדה **אסור** | חסימה, לא עלות. לכן הורד למוצא אחרון אחרי המאגר המוטמע |

**ביקור ראשון: ~190KB** (7.4MB → 734KB עם WebP → ~190KB אחרי שהרקעים הפכו לווקטור ותיקיית `templates/` הוסרה. מה שנשאר הוא בעיקר הגופנים המקומיים ו-`cities-il.json`).

## נקודות חשובות
- **חותמת גרסה**: הקבוע `BUILD` ב-`app.js`, מוצג בתחתית הדף. לעדכן בכל שינוי.
- **מרוץ טעינת גופנים**: גופני Google לא מוכנים כשהקנבס מצייר, ולכן יש span-ים נסתרים ב-HTML שמכריחים טעינה מוקדמת, `document.fonts.ready`, ובדיקת `document.fonts.check` לפני הציור. בלי זה הכותרת נופלת לגופן חלופי בעוד תת-הכותרת מקבלת את החדש.
- **כיול אופטי לכל זוג גופנים**: לכל גופן גוף שונה באותו `px`, ולכן לכל זוג יש `scale` משלו ומשקל **שהקובץ באמת מכיל** (בקשת משקל שאינו קיים גורמת להדגשה מסונתזת מכוערת).
- **שמות יישובים כפולים**: המאגר הרשמי מכיר את השם הרשמי בלבד — נווה צוף רשומה כ**חלמיש**. לכן יש נורמליזציה של כתיב מלא/חסר (`נווה`↔`נוה`, `קריית`↔`קרית`) **וגם** נפילה ל-OSM לשמות עממיים.
- **דקות הדלקת נרות** הן עניין הלכתי: זיהוי אוטומטי לפי מרחק (ירושלים 40, חיפה 30, אחרת 18) + בורר ידני 15-40 לכל יישוב שנוסף, והמספר מוצג לצד השם.
- **אסור** להטמיע חילוץ מ-OpenStreetMap כקובץ: זה "מסד נתונים נגזר" ב-ODbL ומחייב שחרור באותו רישיון. שאילתה חיה אינה יוצרת חובה כזו (רק ייחוס, שקיים).
- **אסור** להטמיע מפתח Gemini בדף — הוא ציבורי וניתן לשליפה.

## היסטוריית שינויים
| תאריך | שינוי |
|--------|-------|
| 2026-09-01 | **v9 usage-instrumentation**: תשתית למדידת שימוש הציבור, מכובה כברירת מחדל. (1) `usage.js` — הקובץ היחיד בפרויקט ששולח משהו החוצה, מכוון להיות קצר ונקרא בישיבה אחת; עם `ENDPOINT` ריק אין אף בקשה. (2) `collector/` — Cloudflare Worker + D1, שורה לאירוע (KV היה נחנק במסלול החינמי: 1,000 כתיבות ליום מול ~10 לכל לוח). אוצר מילים סגור בכניסה: עיצוב שאינו אחד מהעשרה נדחה, משפט נשמר רק כאינדקס. (3) `dashboard/` — עמוד מקומי בסגנון הבית, נתונים נמשכים ב-`refresh.mjs` עם טוקן מ-Bitwarden; המטען נכתב כ-`stats.js` ולא JSON כי עמוד `file://` אינו רשאי לעשות fetch לקובץ שלידו. באג שנתפס בבדיקה מקצה לקצה: `sendBeacon` עם `application/json` דורש preflight שהוא אינו יכול לבצע, והבקשות נעלמו בשקט — התוכן מסומן `text/plain`. **שמות הנפטרים והמשפטים האישיים אינם נאספים**, כי עמוד הכניסה מבטיח למשפחות ש"אף אחד אחר לא רואה את מה שהזנתם" |
| 2026-09-01 | **v8 search-result-card**: תוצאת חיפוש יישוב נבנתה מחדש ככרטיס מוסבר. שלוש ההחלטות שהמשתמש נדרש להן (השם נכון? דקות ההדלקה נכונות? להוסיף?) נדחסו לשורה אחת, וקבוצת הפקדים גלשה מחוץ לכרטיס — מה שנראה בפועל היה כפתור חתוך בשם "+ ה". כעת כל שדה עם כותרת (`שם היישוב — כך הוא יודפס בלוח`, `הדלקת נרות`), שורת הפעולה עוטפת, ו-`select` קיבל `width:auto` כי `.field select{width:100%}` המשיך לחול עליו. נבדק עד רוחב כרטיס של 220px. בנוסף: `app.js?v=N` בתגית הסקריפט — בלעדיו דפדפן של משפחה חוזרת מריץ את app.js מהשבוע שעבר מול index.html של השבוע הזה |
| 2026-09-01 | **v7 watercolour-depth-city-rename**: (1) **עומק וערבוב צבעים** — הרקעים יצאו שטוחים. כל שכבת צבע נצבעת מעתה תחת `multiply` (ו-`screen` בפלטות הכהות), ולכן חפיפה בין שני כתמים מולידה צבע שלישי במקום להסתיר; כל פלטה קיבלה `c4`, גוון-נגד מהצד השני של הגלגל שכל תפקידו להזין את החפיפות האלה. `wash()` נבנה מחדש כאשכול דיסקים רכים במקום מתאר מלא — מתאר שנצבע בגרדיאנט עגול חתך אותו כשעוד היה בו פיגמנט, וזה מה שהותיר קצוות ישרים וקשים. נוספו `veil` (ציפוי דק על כל הלוח, כדי שהמרכז לא יהיה נייר חשוף), `mottle` (גרעיניות פיגמנט) ו-`sheen` (מקור אור אחד מימין למעלה). `clearCentre` הוחלש בחצי — הלוזנג'ים האדפטיביים כבר אחראים לקריאות. (2) **שם הפרשה** — המקף U+05BE יושב באותו טווח יוניקוד כמו הניקוד, וההסרה הגורפת הדביקה "ניצבים־וילך" ל"נצביםוילך"; המקף נשמר, ושמונה שמות בכתיב חסר עוברים לכתיב מלא. (3) **עריכת שם יישוב** — בתוצאת החיפוש לפני ההוספה ובעיפרון ✎ שברשימה. מפת `renames` מוחלת ב-`allCities()` בלבד, כך שהרשימה והלוח המודפס לא יכולים להיפרד; ובנוסף תוצאת OSM שאיננה מקום בעצמה מדווחת את שם היישוב שבו היא יושבת ("מכולת נווה צוף" ← "נווה צוף") |
| 2026-08-31 | **v6 vector-designs-intro-ui**: שלושה שינויים גדולים. (1) **רקעים וקטוריים** — 12 תמונות ה-WebP הוחלפו ב-10 קומפוזיציות אמורפיות המצוירות בקוד (`mist halo bloom waves dunes strata aurora veins orbit arch`) × 14 פלטות, כולל שתי פלטות כהות שבהן הנייר והדיו מתחלפים בתפקידים. 140 רקעים במקום 12×4, אפס בייטים ברשת, ותמונות הגלריה הן רינדור אמיתי. תיקיית `templates/` (924KB) הוסרה. הגדרות ישנות ממופות אוטומטית (`LEGACY_DESIGNS`) כך שמשפחה קיימת פותחת ורואה את הלוח שלה. (2) **עיצוב מחדש של האשף** — מערכת עיצוב מבוססת CSS custom properties, Heebo לממשק מול Frank Ruhl לכותרות, פס התקדמות עם שם השלב, שדות וכפתורים מודרניים, גלריית עיצובים עם קנבסים חיים ובורר גוונים בעיגולי צבע. (3) **דף כניסה מסביר** — מבקר שהגיע מלינק מועבר לא הבין מה הכלי (אחד אף העלה תמונה של עצמו), ולכן המסך הראשון מציג לוח לדוגמה מרונדר מהצינור האמיתי, שלושה צעדים, והבהרה חוזרת שהתמונה היא של הנפטר/ת |
| 2026-08-28 | **v5 auto-save-backup-nudge**: הובהר שההתמדה כבר אוטומטית (`localStorage` = המקבילה לקוקיס) — `touchSettings()` נקרא בכל יצירת לוח כך שהמצב שהפיק את התמונה הוא זה ששורד. שני כפתורי הקובץ הוסרו מהמסך הראשי (חזר ל-4 כפתורים) ועברו לאשף/הגדרות, ונוספה **הצעת גיבוי חד-פעמית** אחרי הלוח הראשון עם דגל `memorialBoard.backupHinted` — כי אחסון הדפדפן ניתן לניקוי, ומה שיאבד הוא התמונה |
| 2026-08-28 | **v4 lean-assets-local-cities**: הפרויקט נותק מ-נבנצל, שמו שונה ל-**Zmanei_Nezach** (תיקייה, ריפו, כותרת, אייקון). נכסים: JPEG→WebP + תמונות ממוזערות 320px, ביקור ראשון ירד מ-7.26MB ל-734KB. הוטמע `cities-il.json` (1,174 יישובים) עם חיפוש מקומי ונורמליזציית כתיב, ו-Nominatim ירד למוצא אחרון. נוסף ייצוא/ייבוא קובץ הגדרות במקום חשבון משתמש. כתובת קשר בתמונה הנוצרת + כפתור פנייה. הוסרו נווה יאיר, תמונת יאיר ושמו מברירות המחדל |
| 2026-08-28 | **v3 city-search-osm**: נפילה ל-Nominatim/OSM ליישובים קטנים (נבדק 12/12), אזור זמן מקואורדינטות, בורר דקות הדלקת נרות, 5 עיצובים שבתיים (נרות, חלות, שולחן, הבדלה, רימונים), 4 גווני צבע, גלריה מקובצת |
| 2026-08-28 | **v2 fonts-cities-design**: חיפוש ערים, 3 עיצובים נוספים, 5 זוגות גופנים עם כיול אופטי, הלבשת תמונה עדינה עם זום ותצוגה מקדימה חיה |
| 2026-08-28 | **v1 builder-mvp**: אשף 5 צעדים, 4 תבניות, הגדרות ב-localStorage, מודאל מקורות הזמנים |
